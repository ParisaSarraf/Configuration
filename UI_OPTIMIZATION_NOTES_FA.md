# گزارش بهینه‌سازی UI/UX

## تغییرات اعمال‌شده
- لایه‌ی اصلی، هدر و سایدبار برای موبایل، تبلت، دسکتاپ و نمایشگرهای بزرگ بازطراحی ریسپانسیو شدند.
- کنترل overflow در سطح صفحه، جدول، فرم، مودال، تب، چارت و کانتینرها اضافه شد.
- اندازه‌ها با الگوریتم Compact آنت‌دیزاین فشرده‌تر شدند و حداقل اندازه‌ی مناسب لمس در موبایل حفظ شد.
- جدول مشترک اکنون حالت خالی کاربرپسند و ورودی‌های اختیاری `error` و `onRetry` دارد.
- کامپوننت‌های مشترک Loading، Error و Empty برای استفاده در صفحات داده‌محور اضافه شدند.
- Error Boundary سراسری برای جلوگیری از سفیدشدن کامل برنامه اضافه شد.
- پیام خطاهای شبکه و HTTP به متن‌های قابل‌فهم فارسی تبدیل شدند.
- منطق تجاری، APIها، محاسبات و handlerهای موجود تغییر نکردند.

## اعتبارسنجی
تست‌های مستقل Node بدون خطا اجرا شدند. اجرای build کامل در محیط تحویل به دلیل غیرفعال بودن دسترسی شبکه و موجود نبودن dependencyهای npm در cache ممکن نشد؛ بنابراین پس از `npm ci`، دستورهای `npm run build` و `npm test` را در محیط توسعه اجرا کنید.
