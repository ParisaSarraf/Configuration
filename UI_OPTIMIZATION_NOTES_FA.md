# گزارش ری‌دیزاین یکپارچه UI/UX

## Design System ثابت
- فقط یک تم روشن و ثابت در پروژه فعال است؛ سوییچ تم و منطق دارک‌مود حذف شد.
- منبع اصلی توکن‌ها: `src/styles/design-system.css`
- تنظیمات هماهنگ Ant Design: `src/App.jsx`
- تنظیمات هماهنگ Tailwind: `tailwind.config.js`
- مستند کامل توکن‌ها: `DESIGN_SYSTEM_FA.md`

## یکپارچه‌سازی سراسری
- همه رنگ‌های پراکنده به پنج خانواده محدود تبدیل شدند: `blue` برای برند/info، `emerald` برای success، `amber` برای warning، `red` برای error و `slate` برای neutral.
- تایپوگرافی به پنج سطح Caption، Body، Subtitle، Title و Display محدود شد.
- فاصله‌گذاری بر مبنای grid چهارپیکسلی، سه radius و سه سطح shadow استاندارد شد.
- Button، Input، Select، Table، Card، Modal، Tabs، Badge، Tag، Tree، Upload و Pagination از یک ظاهر واحد پیروی می‌کنند.
- صفحات اصلی، فرم‌ساز، فرآیندساز، گزارش‌ها، تنظیمات، کارتابل، صفحات محصول و مودال‌ها از توکن‌های مرکزی استفاده می‌کنند.

## ریسپانسیو و ابعاد
- اسکرول افقی فقط داخل جدول/ناحیه داده عریض باقی می‌ماند و صفحه overflow نمی‌گیرد.
- فرم‌ها و مودال‌ها در موبایل fit می‌شوند.
- کنترل‌ها در دسکتاپ 36px و اهداف لمسی در موبایل حداقل 44px هستند.

## وضعیت داده و خطا
- loading سراسری React Query، خطای فارسی، Retry، Empty state، وضعیت قطع اینترنت و Error Boundary یکپارچه هستند.

## حفظ رفتار
- قرارداد API، منطق تجاری و state management تغییر نکرده‌اند؛ تغییرات در لایه UI/UX و بازخورد خطا محدود شده‌اند.
